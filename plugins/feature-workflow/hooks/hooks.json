{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/crew-state.py\" session-brief --cwd \"${CLAUDE_PROJECT_DIR}\" | python3 -c 'import json,sys; t=sys.stdin.read().strip(); sys.stdout.write(json.dumps({\"hookSpecificOutput\": {\"hookEventName\": \"SessionStart\", \"additionalContext\": t}})) if t else None'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
